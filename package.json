{
  "name": "tennu-dbcore",
  "version": "3.0.0",
  "description": "A plugin for the tennu IRC framework, required by my other db driven plugins.",
  "main": "plugin.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/Tennu/tennu-dbcore"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "git push && git push --tags && npm publish"
  },
  "author": "Victorio Berra",
  "license": "ISC",
  "dependencies": {
    "knex": "*"
  }
}
